<?php
namespace xhtml;
/**
 * Division \<div>. Defines a block of HTML.
 * Commonly used to apply CSS to a chunk of a page.
 *
 * The Document Division (\<div>) HTML element is generic container for flow
 * content, which does not inherently represent anything.
 *
 * It can be used to group elements for styling purposes (using the class or
 * id attributes), or because they share attribute values, such as lang.
 *
 * It should be used only when no other semantic element
 * (such as \<article> or \<nav>) is appropriate.
 *
 * @example {
 *  <div class="good_stuff">
 *      <h2> This should look good </h2>
 *      <p>
 *          This is a good content
 *      </p>
 *      <p>
 *          End of this context
 *      </p>
 *  </div>
 *  <div class="another_stuff">
 *      <h2> This is another subject </h2>
 *      <p>
 *          Prety cool.
 *      </p>
 *  </div>
 * }
 *
 * @link http://htmldog.com/reference/htmltags/div/
 * @link https://developer.mozilla.org/en/HTML/Element/div
 */
class Div extends abstractEntity\FlowEntity
{
    /**
     * XML Tag Name
     *
     * @var string
     */
    protected $strTag = 'div';

}
?>